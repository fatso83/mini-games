{
  "name": "browser-snake",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-launcher.mjs",
    "build": "tsc --noEmit && vite build",
    "deploy": "npm exec wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.0",
    "@cloudflare/workers-types": "^5.20260923.1",
    "@types/node": "^24.0.0",
    "@types/qrcode": "^1.5.6",
    "jsdom": "^26.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.137.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
